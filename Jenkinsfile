pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the code'
            }
        }
        stage('Test') {
            steps {
                echo 'Running the test for the code'
            }
        }
    }
}
